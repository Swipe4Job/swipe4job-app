<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
<!--    <style name="Theme.AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar"/>-->
    <style name="Theme.Navigation_templateTheme" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Only for xml, not for jetpack compose. -->
    <!-- Customize your theme here. -->

    <!-- Afegit per canviar l'estil del textview als layouts XML on sigui necessari. -->
    <!--<style name="Theme.MenuLayouts.MyTextView" parent="Widget.AppCompat.TextView">
        <item name="android:fontFamily">cursive</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textStyle">bold</item>
    </style>-->
</resources>